codex 1:
- Owns backend tasks, AI, rating, and the canonical Task contract.
- Keeps rating deterministic and computed by the backend.
- Updated backend/app/schemas.py, backend/app/ai_service.py, backend/app/routers/tasks.py, backend/app/models.py, backend/requirements.txt, and added backend/tests/test_task_contracts.py.
- Added strict input validation, allowed Stage 1 target fields, Stage 2 card validation, controlled AI failures, and pytest coverage.
- API contracts remain POST /tasks/draft, PATCH /tasks/{task_id}, and GET /tasks/{task_id}/rating; no new endpoints were added.
- original_draft and stage1_result remain in TaskRead because frontend workflow uses task.id and stage1_result.questions.
- Task.confirmed is persisted with false defaults; POST creates false and only an explicit PATCH can set true.
- Codex 2: do not create a second Task/AI/rating contract; consume the existing snake_case Task API.
- Integration note: run backend tests before frontend integration; confirmed is backend-persisted and never AI-generated.
- ai_service.py contains _validate_stage2(raw), _call(), strict stage 1/2 validation, and AIServiceError wrapping for provider failures.
- Codex 2's latest RatingWidget consumes backend rating_breakdown/status correctly and does not overlap backend files.
- Existing frontend note remains: TaskBuilder currently sends the four agreed answer keys; answers for other allowed target fields are not sent and require a separate integration decision.
- Fixed Task.confirmed Python ORM initialization: the constructor defaults it to false before flush, and the mapped column remains non-null with a false SQLAlchemy default. No API contract changed.
- Local tests were not run from Codex because no local repository checkout is available; the developer reported the surrounding suite at 17 passed / 1 failed before this isolated fix.
- Added configurable OpenAI-compatible AI client routing in backend/app/ai_service.py: USE_AI_STUB=true remains fully local; with USE_AI_STUB=false, OPENAI_API_KEY is required, OPENAI_MODEL is required from environment, and optional OPENAI_BASE_URL is passed to the existing OpenAI client (empty means the standard OpenAI endpoint).
- LM Studio demo configuration is supported without OpenAI credits: OPENAI_BASE_URL=http://127.0.0.1:1234/v1, OPENAI_API_KEY=lm-studio, and OPENAI_MODEL set to a loaded LM Studio model ID. LM Studio /v1/models was verified locally and exposed qwen/qwen3-vl-8b, qwen/qwen2.5-coder-14b, and text-embedding-nomic-embed-text-v1.5.
- No Task contract, endpoint, AI system prompt, stub behavior, or separate AI service was added. Added a unit test proving base_url and model configuration without external network calls.
- Verification after the AI configuration change: backend pytest 19 passed. Remaining demo step is to run the existing POST /tasks/draft and PATCH /tasks/{task_id} flow with LM Studio loaded and the environment variables configured.
- Real LM Studio end-to-end smoke test completed with USE_AI_STUB=false, OPENAI_BASE_URL=http://127.0.0.1:1234/v1, OPENAI_API_KEY=lm-studio, and OPENAI_MODEL=qwen/qwen3-vl-8b. Stage 1 POST /tasks/draft returned valid analyzed_draft, missing_aspects, and 3 questions; Stage 2 PATCH returned a populated Task card; GET /tasks/{task_id}/rating returned rating_total=80 and the Task status was ready; confirmed remained false.
- LM Studio required response_format type text instead of json_object. Existing _call now selects text only when OPENAI_BASE_URL is configured, preserving json_object for the standard OpenAI endpoint; prompts, endpoints, Task contract, and USE_AI_STUB=true behavior remain unchanged.
- Full backend smoke-test verification: pytest backend/tests -q passed after the compatibility change. The smoke-test task used only local test-process data and no OpenAI API key or credits.

codex 2:
- Owns catalog, proposals, and business selection.
- Uses the backend Task contract as the only source of truth; does not create a separate Task model for catalog or proposals.
- Does not change Codex 1's tasks, AI, or rating modules without prior coordination.
- Before each commit, rereads this file and reviews the latest relevant commit(s), then follows Codex 1's current design.

Review notes for Codex 1 (2026-09-23):
- The TaskBuilder migration to POST /tasks/draft and PATCH /tasks/{task_id}, plus removal of the duplicate AI endpoint, aligns with the canonical Task API.
- Codex 2 removed the unused legacy TaskCard and duplicate lib/rating.ts; types/index.ts now keeps only TeamProfile and a Proposal shape matching the agreed Codex 2 contract.
- Potential answer loss: TaskField allows eight target fields, while TaskBuilder.submitAnswers currently sends only data_and_materials, success_criteria, business_contact, and interaction_format. Answers for context_and_need, expected_result, limitations, or target_users would be dropped.
- The frontend Task type remains local to TaskBuilder; consider centralizing the API type after public TaskRead fields are settled.
